; main.opcode — Conceptual boot-level ops for TactixCore Reborn
; Emulated/Simulated format

MOV AX, 'TACTIXCORE'
INT 10H           ; Display startup screen
CALL CHECK_CONF   ; Confirm configs
JZ FAILBOOT

CALL LOAD_SECURITY_MODULES
CALL LOAD_PERFORMANCE_BOOST

CMP MODE, "SAFE"
JZ INIT_SAFE_ROUTINES
JMP INIT_CUSTOM

:INIT_SAFE_ROUTINES
NOP
CALL QUARANTINE_SCAN
CALL AUTO_PROTECT
RET

:INIT_CUSTOM
CALL USER_PROFILE_BOOT
RET

:FAILBOOT
MOV AX, 'CONFIG ERROR'
INT 21H
HLT
